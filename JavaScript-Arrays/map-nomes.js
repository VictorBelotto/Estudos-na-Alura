const nomes = ["vICTOR", "ellen", "MaTeus"];

/* const nomeMaiusculo = nomes.map((nome) =>{
   return nome.toUpperCase()
}) 

console.log(nomeMaiusculo) */

// Da para fazer assim tambem:

const nomeMaiusculo = nomes.map((nome) =>nome.toUpperCase()) 
 
 console.log(nomeMaiusculo)